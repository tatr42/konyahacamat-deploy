"use client";

import { useState } from "react";
// Diğer importların burada kalabilir...

export default function AdminPage() {
  // Senin mevcut admin paneli kodun burada olmalı.
  // Eğer içerik yoksa, test etmek için geçici olarak şunu yapıştır:
  return (
    <div className="p-10 text-white">
      <h1>Admin Paneline Hoş Geldiniz</h1>
      <p>Eğer bu sayfayı görüyorsanız build hatası çözülmüştür.</p>
    </div>
  );
}